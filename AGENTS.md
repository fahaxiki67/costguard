# AGENTS.md — CostGuard 开发代理与协作者必须遵守的规则

> 本文件长期保留项目的最高优先级原则。任何代码变更、重构、依赖引入、文档修改
> 都必须先对照本文件检查是否违反以下原则。冲突时以本文件为准。

## 一、最高优先级原则（永不变更）

1. **准确性优先于自动化率。**
2. **原始文件默认只读**：任何情况下不得直接修改、覆盖或删除用户原始文件。
3. 生成结果使用独立工作目录。
4. 所有计算必须可追溯。
5. 所有关键结论必须能够追溯至：`原始文件 → Sheet/页码 → 行列/段落 → 原始内容`。
6. 缺失数据不得自动填 0。
7. 无法比较的数据不得强行比较（标记为 `不可比`）。
8. 无法匹配的数据不得为了让金额一致而人为调平。
9. 所有不确定匹配必须显示置信度，并进入人工复核队列。
10. 数量、单价、金额等确定性计算由程序完成，**不允许依赖 LLM 心算**。
11. AI（LLM）只适合：名称语义识别、条款理解、归类、风险提示、文本生成；
    **不得替代底层确定性计算**。
12. 每个最终结果必须同时保留计算过程和证据来源。
13. 涉及金额的数据至少进行一次独立交叉校验。
14. 系统必须允许用户随时人工修正 AI 的匹配结果，并记录修改原因。

## 二、不可替代的数据纪律

- 金额、数量、单价计算一律使用 `Decimal`，禁止 `float` 参与金额运算（见 `core/engine/money.py`）。
- 缺失值必须显式标记为 `待补资料` / `不可比` / `无法确认`，**不得自行补值、归零或调平**。
- 匹配置信度分级固定为五档：`确认匹配 / 高概率匹配 / 疑似匹配 / 不可比 / 待补资料`。
- 不允许因为名称相似就直接合并清单。

## 三、敏感信息隔离（最高等级安全要求）

- `local_private_data/` 永远不进入 Git（已在 `.gitignore` 中隔离）。
- 真实合同、真实结算文件、真实企业经营数据、真实人员信息严禁提交。
- 严禁提交：API Key、Token、Password、Apple/Windows 签名证书、私钥、
  含敏感信息的本地绝对路径。
- 仓库中测试数据只能使用 synthetic / 匿名化 / 人工构造数据。
- pre-commit 与 CI 必须包含敏感信息扫描。

## 四、平台策略

- 平台顺序固定：P0 = macOS Apple Silicon (arm64)；P1 = Windows x64；P2 = 待定。
- 禁止分别开发两个独立软件：共享核心引擎 + 共享数据模型 + 共享测试 + 共享 UI + 平台专用打包/适配层。
- 核心业务代码（`src/costguard/core/`）必须平台无关，不得 import 平台私有 API；
  平台差异只能进入 `src/costguard/platform/`。
- 不得因为 Windows 后续需求阻碍 Mac 主版本推进；也不得为炫酷 UI 引入过重 Web 服务架构。

## 五、开发方法

- 禁止一次性堆砌整个系统；按 `ROADMAP.md` 的 Phase 推进。
- 每个 Phase 必须完成：设计 → 实现 → 单元测试 → 集成测试 → Review → 修复 → 回归测试 → 文档。
- 完成一个 Phase 后，如无真正需要用户决策的阻塞问题，继续下一阶段；
  不因普通技术细节停下等待确认。
- 例外（必须停下确认）：删除或覆盖用户原文件、无法恢复的数据操作、
  系统级高风险权限、安装来源不明的软件、真实敏感资料上传外部服务。
- 重要阶段建立 Git commit；不做与当前阶段无关的大规模重构；
  关键架构决策写入 ADR（`docs/adr/`）。

## 六、质量要求

"程序能跑"不等于完成。必须追求：正确、可测试、可追溯、可解释、可复核、可恢复、可维护。

- 不得只测试正常数据；`synthetic_test_data/` 必须持续扩充异常用例
  （多层表头、合并单元格、隐藏行列、文本数字、千分位、¥ 符号、负数、公式错误、
  重复、漏项、单位转换、名称近似/语义相同/貌似相似、税率变化、舍入差异等）。
- 涉及金额的核心函数重点做 property-based testing 与边界条件测试（hypothesis）。
- 软件数据与用户工程数据严格分离；升级不得影响用户数据库与项目资料；
  数据库带 Schema Version，Migration 前自动备份，失败可回滚。

## 七、升级与发布

- 升级策略：提示发现新版本 → 用户查看 Release → 用户主动决定升级；
  不得后台静默下载/执行新程序。
- 发布物：`CostGuard-x.y.z-macos-arm64.dmg`、`CostGuard-x.y.z-windows-x64.exe`
  与 `SHA256SUMS.txt`；版本遵循 SemVer。
- 源码采用 Apache License 2.0（见 `docs/adr/ADR-009`）；真实工程资料和
  `local_private_data/` 不属于开源内容，永远不得提交或发布。
