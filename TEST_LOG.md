# 测试日志

本文件只记录脱敏的测试范围、结果和恢复入口；真实金额、原始路径、原始文件内容和私有运行 JSON 不写入 Git。

## 2026-09-08 01:45：混合语料完整验收

- 运行：`run_20260908_014510_884482`（完整结果保存在被忽略的 `local_private_data/real_acceptance/work/`）。
- 语料：二道沟主集、西航港样本、双江变电站样本，共 23 个副本。
- 结果：23/23 导入成功；处理前后哈希一致；未发现副本被修改；仅二道沟 T-ERG-08 完成技术结算链路。
- T-ERG-08：人工确认后状态快照与数据库一致；明细/小计总计行数一致；C 来源落到原始 Sheet 单元格；跨层差异仍开放；WPS/Excel 仍待人工复核。
- 运行期间出现既有 OLE2 inconsistency 警告，命令正常退出；该警告不等于 WPS/Excel 复核通过。

## 2026-09-08 02:00：跨项目人工确认探针

### 西航港 T-XHG-03

- 选择 Sheet：道路工程产值明细表；人工确认多层表头和原始数据范围。
- 结果：成功抽取候选明细；报告保留筛选条件、合并单元格和范围完整性风险。
- 其余 Sheet 仍待角色/映射确认，未强行进入结算计算。

### 双江 T-SJ-06

- 选择 Sheet：KA 土建工程；人工确认多层表头和原始数据范围。
- 结果：成功抽取候选明细；报告保留明细区合并单元格风险。
- 其余 Sheet 仍待角色/映射确认，未强行进入结算计算。

两组探针均使用相同人工列映射和范围独立重放两次，状态、候选行数、结构风险和剩余人工门控完全一致；脱敏摘要保存在被忽略的 `local_private_data/real_acceptance/generalization_probes_20260908.json`。

### 2026-09-08 02:10：清单页变更/公式边界探针

#### 西航港 T-XHG-05（独立人工探针，不等同于完整运行）

- 选择 Sheet：`计量计价清单明细`；人工确认表头 4-5 行、数据范围 6-132 行，按本期工程量/单价/金额列抽取。
- 结果：105 行候选，其中 102 行工程量明细、1 行税金/费用、2 行汇总；文件仍为 `partial`，筛选范围 `A5:T132`、7 个隐藏列和 3 组明细区合并单元格已留痕。
- 结构证据：209 个公式缓存未验证单元格、5 个源表公式错误缓存；金额不作为确定性依据，状态保持 `pending`。

#### 双江 T-SJ-07（独立人工探针，不等同于完整运行）

- 选择 Sheet：`1#变更（土建工程）`；人工确认表头 3-4 行、数据范围 6-157 行，按变更清单列抽取。
- 结果：139 行候选明细；文件仍为 `partial`，筛选范围 `A1:O159`、13 组明细区合并单元格已留痕。
- 结构证据：9 个公式缓存未验证单元格且均为除零错误缓存；未证明筛选/隐藏状态不影响结果，状态保持 `pending`。

两页均使用临时隔离项目各独立重放两次；候选行数、状态、范围和结构风险完全一致；原始副本 SHA-256 处理前后相同。最新完整验收对两页仍为 `parse=false`/`needs_manual_review`，探针结果不应表述为完整运行已经完成结算链路。金额和数量仅保存在被忽略的私有摘要中，不形成业务结论。

### 2026-09-08 02:12：真实总计行回归修复

- 复现：西航港 `计量计价清单明细` 的“合计（含税）/合计（不含税）”位于首列；修复前 `is_subtotal_row` 未识别税口径后缀，真实重放会把总计行留在候选明细路径。
- 红测试：`tests/unit/test_header_detect.py::TestSubtotal::test_tax_qualified_total_in_leading_column` 在修复前失败。
- 根因修复：`_SUBTOTAL_TRIM` 增加 `含税|不含税`，只扩展税口径后缀识别，不改变金额或源数据。
- 同一红测试修复后通过；相关表头、行抽取、结算管线、C 路径测试通过。
- 修复后真实重放：105 行候选，其中 103 行明细、2 行汇总行；各独立重放两次一致，原始副本哈希未变化。

### 2026-09-08 02:20：修复后完整私有验收

- 运行：`run_20260908_022047_096540`（完整结果保存在被忽略的 `local_private_data/real_acceptance/work/`）。
- 结果：23/23 文件导入成功；处理前后副本 SHA-256 一致；无副本被修改。
- T-ERG-08：148 组技术复算、47 条异常、134 项匹配、226/226 证据可溯源；`technical_execution_complete=true`、`verification_level=insufficient`、`control_status=difference_open`、`wps=pending_manual`、整体 `pending_wps_with_findings`。
- 既有 OLE2 inconsistency 警告仍出现，命令正常退出；该警告不等于 WPS/Excel 复核通过。

### 2026-09-08 02:49：独立复核收口

- 复核提交：`19d8248`；结论：解析规则修复有条件通过，正式资料验收/生产发布仍不通过。
- 复核确认 XHG-05/SJ-07 的结果来自临时隔离人工探针，不能写成最新完整运行已自动完成结算链路；文档已补充该边界。
- 复核确认 XHG-05 应拆分工程量明细、税金/费用行和汇总行；私有探针摘要已同步修正，未修改源文件和原始金额。
- WPS/Excel 实机复核、范围完整性、A/B 独立性及跨层桥接差异仍列为待补证；本次不扩大税价标签识别范围。

### 2026-09-08 03:00：旧版 XLS 前缀税口径总计修复

- 真实样本：二道沟 T-ERG-05 的旧版 `.xls` 工作表 `材料调差`；页末标签为“2024年1月份材料调差合计（含税/不含税）”。
- 复现：修复前两个标签未被 `is_subtotal_row`/`is_grand_total_row` 识别，尾部数值行会被当作孤立尾注剔除，无法形成控制行证据。
- 红测试：`tests/unit/test_header_detect.py::TestSubtotal::test_prefixed_tax_qualified_total` 修复前失败。
- 根因修复：增加精确的“合计/总计 + （含税/不含税）”末尾模式；保留“合计（含9.313%税价）”负例不识别，未扩大为任意税率文本。
- 同一红测试及表头、行抽取、C 路径、覆盖证明和验收/UI 相关测试修复后通过。
- 真实隔离重放两次：导入成功，整体 `partial`/待人工，`材料调差` 保留 9 行明细和 2 行汇总；旧版 XLS 公式缓存、合并单元格拓扑和筛选可见行仍无法由当前解析器证明。

### 2026-09-08 03:02：修复后完整私有验收

- 运行：`run_20260908_030208_074502`（完整结果保存在被忽略的 `local_private_data/real_acceptance/work/`）。
- 结果：23/23 文件导入成功；处理前后副本 SHA-256 一致；无副本被修改。
- T-ERG-05 的 `材料调差` 从修复前 `n_subtotal=0` 变为 `n_subtotal=2`，其余旧版 XLS 结构性门控保持；T-ERG-08 的技术复算、候选控制值、开放桥接差异和 WPS 待人工状态未发生回归。
- 既有 OLE2 inconsistency 警告仍出现，命令正常退出；该警告不等于 WPS/Excel 复核通过。

### 2026-09-08 03:15：旧版 XLS 同类页泛化探针

- 对二道沟 T-ERG-06、T-ERG-07 的 `材料调差` 页使用同一隔离 `inspect_file` 流程各重放两次。
- T-ERG-06：9 行明细、2 行汇总；T-ERG-07：11 行明细、2 行汇总；两页导入成功、整体 `partial`/待人工，重复结果一致。
- 结果支持该规则覆盖同类旧版 XLS 的精确税口径总计标签，但不解除旧版 XLS 公式缓存、合并单元格拓扑和筛选可见行门控；未形成正式金额结论。

### 2026-09-08 03:20：二道沟材料采购页人工映射探针

- 真实样本：T-ERG-14 `二道沟物资采购数据汇总表.xlsx` 的 `涵管` 页；人工确认单行表头第 2 行、数据第 3-4 行及列映射。
- 隔离 `inspect_file` 重放两次：导入成功，目标页均为 `confirmed`、2 行明细；其余 3 个 Sheet 保持角色待确认，整体 `partial`/待人工。
- 该探针验证人工映射后的材料明细抽取和剩余角色门控，不形成材料采购或结算金额结论。

### 2026-09-08 03:22：提交绑定的完整私有验收

- 运行：`run_20260908_032216_052490`；运行命令、Git commit、代码树哈希、源码哈希和工作树洁净状态保存在被忽略的 `local_private_data/real_acceptance/acceptance_run_manifest_20260908.json`。
- 绑定提交：`07d7a265`；23/23 文件导入成功，处理前后副本 SHA-256 一致，修改副本为空。
- T-ERG-05 `材料调差` 仍为 9 行明细、2 行汇总且整体待人工；T-ERG-08 仍为 `pending_wps_with_findings`、`difference_open`，没有把提交绑定误写成正式业务通过。
- 报告期次表的字段已改为“候选控制内部状态”，并明确该状态只描述当前文件内 A/B/C 候选路径关系，不代表与其他源表或汇总层一致。

### 2026-09-08 03:36：历史提交绑定验收（旧报告标签）

- 运行：`run_20260908_033618_403290`；私有运行清单已更新为提交 `ac2f110`、对应代码树和源码 SHA-256，工作树洁净。
- 结果：23/23 文件导入成功，处理前后副本 SHA-256 一致，修改副本为空；T-ERG-05 仍为 9 行明细、2 行汇总且待人工，T-ERG-08 仍为 `pending_wps_with_findings`、`difference_open`。
- 该轮原始报告仍使用旧标签“候选控制内部状态 / A/B/C候选内部一致”；它不作为新标签修复的证据。后续提交 `72b5a25` 绑定的 `run_20260908_065222_289842` 才验证“A/C候选控制一致”，A/B 结果另列。

### 2026-09-08 03:45：跨项目探针不可变清单

- 为 XHG-05 `计量计价清单明细`、SJ-07 `1#变更（土建工程）` 补充私有清单 `generalization_probe_manifest_20260908.json`。
- 清单固定记录代码提交 `19d8248`、代码树哈希、源文件 SHA-256、人工表头/数据范围、`inspect_file` 隔离重放命令、两次退出码和输出摘要；两页均 `replay_same=true`、源哈希未变化。
- 清单只增强探针可追溯性；两页仍为 `partial`/结构门控待人工，未形成跨项目结算或金额结论。

### 2026-09-08 03:50：报告语义历史复核

- 独立复核发现 03:36 历史运行仍保留旧标签；当前生产代码和后续提交绑定运行已改为“候选控制内部状态 / A/C候选控制一致”，并明确不把候选控制比较写成跨层源表一致。
- 最新 T-ERG-08 仍保持跨层差异开放、WPS/Excel 待人工、校核不充分和不得形成正式业务结论的边界；当前代码提交无阻断。
- 管理层摘要 DOCX 未单独重复展示跨层差异；差异已在 Markdown 报告、审核底稿和证据索引中保留。若未来要求 DOCX 脱离底稿独立流转，再单独补模板字段。

### 2026-09-08 04:00：材料核销分析角色门控探针

- T-ERG-15 `二道沟项目材料核销分析_量价盈亏及调差口径.xlsx` 在临时隔离项目中只读重放两次；两次均导入成功、整体 `partial`，6 个 Sheet 全部保持角色待确认。
- 根因是文件名含“汇总/核销/台账”语义，系统按文件级角色门控，不把分析汇总表强行抽取为结算明细；结果与预期 fail-closed 行为一致。
- 源文件哈希、代码提交/树哈希和两次输出摘要保存在被忽略的 `local_private_data/real_acceptance/role_gate_probe_20260908.json`；未修改原始资料，不形成材料核销或结算金额结论。

### 2026-09-08 04:15：双江财务台账角色门控探针

- T-SJ-08 财务付款台账在临时隔离项目中只读重放两次；两次均导入成功、整体 `partial`，21 个 Sheet 全部保持角色待确认。
- 台账类多 Sheet 文件未被强行写入结算明细；源文件哈希、代码提交/树哈希和两次输出摘要保存在被忽略的 `local_private_data/real_acceptance/ledger_role_gate_probe_20260908.json`。
- 该探针只验证台账边界和 fail-closed 门控，不形成付款、结算或责任结论。
- 独立复核确认私有清单、状态记录和审计说明一致，无代码或证据边界阻断。

### 2026-09-08 04:30：西航港混合合同清单角色门控探针

- T-XHG-02 旧版合同清单包含封面、汇总、F.1、措施、暂估价和税金等多角色 Sheet；在临时隔离项目中只读重放两次。
- 两次均导入成功、整体 `partial`，22 个 Sheet 全部保持 `pending`；其中 16 个进入角色待确认、1 个进入表单待确认，未强行写入结算明细。
- 源文件哈希、代码提交/树哈希和两次输出摘要保存在被忽略的 `local_private_data/real_acceptance/mixed_contract_role_gate_probe_20260908.json`；OLE2 警告来自既有旧版 XLS 格式，不改变门控结论。

### 2026-09-08 06:59：报告语义修复后的提交绑定完整验收

- 运行：`run_20260908_065222_289842`；私有运行清单绑定提交 `72b5a25`、代码树哈希和关键源码 SHA-256，运行时工作树洁净。
- 结果：23/23 文件导入成功，处理前后副本 SHA-256 一致，修改副本为空；T-ERG-08 仍为 `pending_wps_with_findings`、跨层差异开放，未形成正式业务结论。
- 最新 Markdown 报告已显示“候选控制内部状态 / A/C候选控制一致”，A/B 结果另列；该标签修复未修改金额、原始资料或验收门控。

## 回归验证

- `QT_QPA_PLATFORM=offscreen uv run pytest -q`：全套测试退出码 0（100%，3 个跳过）；仅有既有 `zipfile` 重复条目警告，无失败。
- `tests/unit/test_acceptance_runner.py`
- `tests/unit/test_ui_sheet_inventory.py`
- `tests/unit/test_ui_workbench.py`
- `tests/unit/test_reporting_summary.py`
- 结果：相关测试组退出码 0；独立复核代理对核心验收执行器和 Sheet 清单测试记录为 43 passed。
- `uv run ruff check ...`：通过。
- `python3 -m py_compile ...`：通过。
- `git diff --check`：通过。

## 已修复问题

1. 人工决定后报告继续使用确认前 Sheet 快照，已改为读取当前数据库状态和实际行数。
2. A/B 结果一致被表头暗示为独立复核，已改为“结果一致性”，并显示共享抽取器限制。
3. C 候选控制值来源不清、跨层桥接未显式展示，已增加来源和开放差异记录。
4. 普通 `pending` 状态被泛化为“待复核”，已按角色确认、人工映射、表单和结构风险细分。
