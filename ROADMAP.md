# ROADMAP.md — Jiadun（价盾）开发路线图

> 平台顺序固定：P0 macOS Apple Silicon → P1 Windows x64 → P2 待定。
> 每个 Phase 完成：设计 → 实现 → 单元测试 → 集成测试 → Review → 修复 → 回归 → 文档。

## 产品名称与兼容边界

当前产品名称为 **Jiadun（价盾）**，机器可读的命令和 Python 包名为 `jiadun`。
GitHub 仓库地址、已有 v0.x tag、Release、远端资产和历史项目目录仍保留
`CostGuard` 名称，作为可追溯的兼容事实；更名和升级不得删除或覆盖原始资料。
后续构建物统一采用 `Jiadun-<version>-macos-arm64.dmg`、`Jiadun.app` 等现行名称。

## 状态图例
✅ 完成 | 🔨 进行中 | ⬜ 未开始

| Phase | 内容 | 状态 |
|-------|------|------|
| 0 | 总体架构与跨平台设计：目录骨架、ADR、数据模型、风险自检 | ✅ |
| 1 | macOS ARM64 最小可运行框架：平台层、DB+迁移、项目管理、PySide6 壳 | ✅ |
| 2 | Excel 导入和结构化：解析保真层、表头识别、清洗、合成测试数据 | ✅ |
| 3 | 结算计算引擎：期次累计、加权平均单价、双向校核（迁移v2、v3） | ✅ |
| 4 | 异常检测：23+ 规则引擎（21条规则，含舍入差异分级） | ✅ |
| 5 | 证据链：Evidence ID、审计日志、匹配五档置信度 | ✅ |
| 6 | 合同和合规分析（docx/pdf/txt 条款结构化，原文引用，风险清单） | ✅ |
| 7 | 成果导出：12 类报表、Excel 保留公式、结构兼容 WPS（真机三环境专项待验收） | 🔨 |
| 8 | 完整 Mac UI（项目页+五 Tab 工作台） | ✅ |
| 9 | Mac 大规模测试和性能优化（1万/5万/20万行基准） | 🔨 |
| 10 | Mac 打包（unsigned DMG） | ✅ v0.1.14 预发行（ad-hoc 签名未公证；公证版见 Phase 14） |
| 11 | GitHub CI/CD（源码 CI 与敏感信息扫描已就绪；macOS 打包工作流已就绪） | 🔨 |
| 12 | Mac Beta Release (v0.9.x) | ⬜ |
| 13 | 真实案例回归测试和 Bug 修复 | ⬜ |
| 14 | Mac V1.0 Release Candidate（Developer ID 签名+公证） | ⬜ |
| 15 | Windows x64 兼容 | ⬜ |
| 16 | Windows 安装包 | ⬜ |
| 17 | Windows Beta | ⬜ |
| 18 | 跨平台 V1.x | ⬜ |

## Phase 0 退出标准
- [x] 目录骨架与 Git 仓库建立，.gitignore 隔离 `local_private_data/`
- [x] AGENTS.md 固化最高优先级原则
- [x] ARCHITECTURE.md（含数据模型、证据链设计、风险对策）
- [x] ADR-001..010
- [x] ROADMAP / README 双语 / 社区文件 / CHANGELOG
- [x] Python 3.12 环境 + 关键依赖导入验证
- [x] Phase 0 commit

## 自动升级策略（Phase 12 起生效）
提示新版本 → 用户点击查看 GitHub Release → 用户主动决定升级。
不实现后台静默更新。

## v0.1.24 生产门槛（未全部满足）

v0.1.24 交付合同事实确认生命周期（阶段 C-1，schema v48）：抽取条款一律候选、
人工确认（推翻须留理由）才是已确认事实、拒绝条款退出运行契约、工作台条款确认
对话框。不改变 macOS Apple Silicon 主平台定位，Windows 特有行为继续隔离在
`platform/` 层。以下门槛未全部关闭前，只能标记为预览候选，不能标记为正式生产版。

- [x] contract_facts 确认生命周期迁移 v48 与历史数据 candidate 回填；
  推翻人工结论必须留理由，全部流转写入审计 Evidence。
- [x] Run Contract 门控：被拒绝事实退出运行契约；候选带状态标记与确认汇总。
- [x] 工作台"合同条款确认…"对话框与 offscreen UI 测试。
- [x] 全量回归以最终退出码收口；黄金基线按先例升 v48 并附基线变更记录。
- [ ] 真实工程扫描 PDF 的中文、表格、印章、旋转页、低质量复印件 OCR 质量回归；
  脱敏黄金样本登记仍为 0。
- [ ] OCR 扫描页候选条款的逐条对照复核入口（按页状态细分）。
- [ ] confirmed 事实接入对上控制基准候选算法（终审/审计报告作为上限候选、
  supersedes 与 CONTROL_CONFLICT 分层）。
- [ ] 真实 Microsoft Excel/WPS（Windows 与 macOS）真机专项、最终包启动与
  取消/异常恢复专项、50k/200k 完整导出基准、Windows 代码签名与 macOS 公证。

## 阶段 C-1：合同事实确认生命周期（进行中）

- [x] 迁移 v48：contract_facts 增加确认状态字段；历史事实回填 candidate，
  不自动升 confirmed。
- [x] 确认/拒绝/待复核服务（推翻须留理由）+ contract_fact_review 审计 Evidence；
  状态变化产生新的 Run Contract 签名。
- [x] Run Contract 载荷带确认状态；被拒绝的事实不进入运行契约；确认情况汇总可见。
- [x] 风险语义：拒绝=缺失；候选=保持覆盖但给出待人工确认提示。
- [x] 工作台"合同条款确认…"对话框（offscreen UI 测试覆盖）。

## 阶段 C-2：PDF 逐页人工对照复核（进行中，schema v49）

- [x] 迁移 v49：pdf_page_reviews 保存每页当前复核决定，历史流转留审计 Evidence。
- [x] 页清单/单页核实（对照依据必填）/退回服务；原生文本页不可复核，
  pending_ocr/failed 文档不适用（fail-closed）。
- [x] 全部应复核页 verified 后文档 needs_review → parsed（写 Evidence + 新签名）；
  条款仍按条款级确认，语义不变。
- [x] 资料中心"逐页对照复核…"对话框（offscreen UI 测试覆盖）。
- [ ] 真实扫描件上验证页级复核闭环的实际可用性（依赖脱敏真实样本）。
- [ ] confirmed 事实接入对上控制基准候选算法（终审报告作为上限候选）。

## v0.1.25 生产门槛（未全部满足）

v0.1.25 交付合同事实确认生命周期（v48）+ PDF 逐页人工对照复核（v49）+ 对上控制
基准候选（v50）+ 框架/管理性协议费率规则（v51）。以下门槛未全部关闭前，只能标记
为预览候选，不能标记为正式生产版。

- [x] 合同事实确认生命周期（candidate→confirmed，推翻须留理由，审计 Evidence）。
- [x] PDF 逐页人工对照复核（对照依据必填，全部应复核页核实才解除文档门控）。
- [x] 对上控制基准候选与五态上限比较（CONTROL_CONFLICT/INCOMPARABLE/PENDING/
  FAIL/PASS；FAIL 只报差额不认定违规责任）。
- [x] 费率规则：多百分比候选抽取 + 公式型候选 + 基数必须人工设定 + Decimal 试算；
  真实框架协议语料实测验证（民权框架协议，隔离副本，原件哈希不变）。
- [x] 全量回归以最终退出码收口；黄金基线按先例升 51 并附变更记录。
- [ ] 真实工程扫描 PDF 的 OCR 质量回归与页级复核实机闭环（脱敏黄金样本登记为 0）。
- [ ] 费率基数与对上/对下期次合计自动关联；费率规则接入 Run Contract 载荷。
- [ ] 控制基准比较结论接入审核问题中心与报告导出；结构化 scope 字段。
- [ ] 真实 Microsoft Excel/WPS（Windows 与 macOS）真机专项、50k/200k 完整导出
  基准、Windows 代码签名与 macOS 公证。

## v0.1.27 生产门槛（未全部满足）

v0.1.27 交付任务 B 服务层（Sheet 角色确认：全 Sheet 清单、角色建议置信度、
重导结转、合同失效联动，schema v54）。以下门槛未全部关闭前，只能标记为
预览候选。

- [x] 全 Sheet 清单服务（名称/可见状态/行列/建议角色/置信度/理由/人工标注）。
- [x] 角色枚举扩展（对上/对下/其他费用）与过滤模式（全部/仅待确认/仅建议参与）。
- [x] 重解析人工决策结转（同名+摘要一致才结转，批量 Evidence）。
- [x] list_kind 纳入 Run Contract（角色变更→签名变化→旧运行失效）。
- [x] 摘要缓存失效触发器（raw_cells 任何写入即删缓存，防漂移失明）。
- [x] 黄金基线升至 schema 54 并附变更记录。
- [ ] Sheet 清单浏览器 UI（反馈#2/#5 界面侧闭环）。
- [ ] 真实工程扫描 PDF OCR 质量回归（脱敏黄金样本 0）。
- [ ] 真实 Microsoft Excel/WPS（Windows 与 macOS）真机专项。
- [ ] 1万/5万/20万行完整导出基准；Windows 代码签名与 macOS 公证。

## v0.1.26 生产门槛（未全部满足）

v0.1.26 交付性能修复（F-3 缓存化，schema v53）、费率规则、PDF 逐页对照复核
和多项稳定性修复。以下门槛未全部关闭前，只能标记为预览候选。

- [x] raw_cell_digest 逐 sheet 缓存（真实数据实测 0.07s vs 2.96s）。
- [x] 迁移运行器 duplicate column 幂等容错。
- [x] 黄金基线升至 schema 53 并附变更记录。
- [ ] 真实工程扫描 PDF OCR 质量回归（脱敏黄金样本 0）。
- [ ] 真实 Microsoft Excel/WPS（Windows 与 macOS）真机专项。
- [ ] 1万/5万/20万行完整导出基准；Windows 代码签名与 macOS 公证。

## 阶段三：对上控制基准候选与上限比较（进行中，schema v50）

- [x] 迁移 v50：control_baselines（金额/税口径/范围/supersedes/确认状态）。
- [x] 候选来源：已确认合同事实（非 confirmed 拒绝）或人工显式登记（出处必填）；
  候选确认必须给出核对依据；supersedes 显式声明。
- [x] 五态上限比较：PASS/FAIL/PENDING/INCOMPARABLE/CONTROL_CONFLICT；
  FAIL 只报超出金额，不认定违规或责任；比较结论写审计 Evidence。
- [x] 对上期次明细合计（Decimal，小计行已在导入时排除）。
- [x] 工作台"对上控制基准…"对话框（登记/确认/比较）。
- [ ] 框架/管理性协议（收取协作方 3%~5% 点位）的独立算法：以协议条款候选 +
  人工确认为来源，生成对上对下分析的结论性框架（另一套算法，不得自动把文本
  比例转成金额规则）。
- [ ] 控制基准比较结论接入审核问题中心与报告导出。
- [ ] 结构化 scope 字段（当前为文本说明比对）。

## v0.1.23 生产门槛（未全部满足）

v0.1.23 聚焦页级 OCR 与混合 PDF 不漏页能力的回归收口和发行卫生修复：补齐 v0.1.22
跳过的发行文档与一致性门槛，把版本断言从测试硬编码改为跟随 `pyproject.toml` 单一
版本源，并以 P0 修复后的全量回归结果作为版本记录依据。不改变 macOS Apple Silicon
主平台定位，Windows 特有行为继续隔离在 `platform/` 层。以下门槛未全部关闭前，
只能标记为预览候选，不能标记为正式生产版。

- [x] P0 修复后全量 pytest 回归以最终退出码收口；失败项逐一定位并修复
  （发行文档缺失、测试版本断言过期），修复后重跑相关集合通过。
- [x] README / README_zh-CN / ARCHITECTURE / ROADMAP 与当前版本一致；
  `docs/RELEASE_NOTES_v0.1.23.md` 按门槛清单格式建立，发行一致性检查可通过。
- [x] 黄金回归：合成案例 PASS、脱敏真实案例保持 PENDING 登记（不冒充）。
- [ ] 真实工程扫描 PDF 的中文、表格、印章、旋转页、低质量复印件 OCR 质量回归；
  脱敏黄金样本登记仍未完成。
- [ ] OCR 合同候选的人工确认界面与 confirmed 才生效的事实生命周期闭环。
- [ ] 对上终审/审计清单作为控制候选的上限关系与冲突分层算法
  （reference / control_candidate / settlement_result 角色分层）。
- [ ] 真实 Microsoft Excel/WPS（Windows 与 macOS）、最终包启动与取消/异常恢复专项、
  50k/200k 完整导出基准、Windows 代码签名与 macOS 公证。

## v0.1.20 生产门槛（未全部满足）

v0.1.20 聚焦资料中心、明确分类和导入过程可观察性；不改变 macOS Apple Silicon
主平台定位，Windows 特有行为继续隔离在 `platform/` 层。以下门槛未全部关闭前，
只能标记为预览候选，不能标记为正式生产版。

- [x] 文件、文件夹和拖拽导入进入后台工作线程，界面不再同步执行大文件解析；已导入
  文件统计可进入资料中心查看，资料分类、方向、状态、来源副本和 SHA-256 可追溯。
- [x] 提供对上/对下合同、结算、终审报告、框架/管理性协议、会议纪要等明确分类；
  未知资料保持待人工分类，不按文件名强行推断。
- [x] 解析不足和扫描型 PDF 进入 `PENDING/OCR 待处理`，不自动生成不可靠的结算、匹配
  或报告结论。
- [ ] 集成可离线运行的 PDF OCR，并保留页码/段落 Evidence；真实 OCR 质量和中文扫描件
  回归仍待验证。
- [ ] 从合同、补充协议、框架/管理协议和会议纪要提取条款候选，加入人工确认后的
  付款比例/管理费规则；不得由 AI 直接决定金额。
- [ ] 对上终审/审计/审核清单与合同清单的控制候选比较、上限关系和冲突分层算法，需
  明确定义“控制基准/参考/结算结果”三种角色后实现。
- [ ] 自动分类、真实 Microsoft Excel/WPS COM、50k 完整导出、签名公证和脱敏黄金案例。

## v0.1.21 当前预发行目标（生产门槛未全部满足）

本节记录当前预发行的开发目标，不改变生产门槛，也不改变
macOS Apple Silicon 主平台战略；Windows 特有实现继续放在 `platform/` 层。

- [x] 建立跨平台页级 PDF 提取边界：逐页覆盖 `1..N`、原生文本/离线 OCR 状态、
  缺页/错序/失败页 fail-closed，避免混合 PDF 静默漏页。
- [x] 接入可离线运行的 RapidOCR 平台适配器；随包模型校验 SHA-256、大小、版本、
  语言和许可证元数据，不静默下载模型、不上传用户文件。
- [x] 将 PDF 页级摘要写入现有 `parse_batches.stats_json`，并把页码、来源 SHA-256、
  提取方式和 OCR 元数据带入合同 Evidence；不新增迁移、不改变 Decimal/原始文件只读。
- [x] OCR 合同候选标记为 `needs_review`，未人工复核前排除出当前 Run Contract。
- [ ] 真实工程扫描 PDF 的中文、表格、印章、旋转页、低质量复印件和多期混合资料回归；
  当前尚未取得可公开纳入仓库的脱敏黄金样本。
- [x] Windows onedir 打包的 OCR 模型资源收集烟测通过（`Jiadun.exe` 和 3 份本地 ONNX
  模型均在产物中）。
- [ ] Windows 最终包启动、取消/异常恢复、真实 Microsoft Excel/WPS 和 macOS Excel/WPS
  专项验证；没有现场证据时保持 `PENDING / NOT VERIFIED`。
- [ ] 合同事实候选的人工确认界面，以及合同/补充协议/框架管理协议/会议纪要的
  规则角色与对上控制基准候选算法；本目标不允许 AI 直接决定金额。

## v0.1.19 生产门槛（未全部满足）

v0.1.19 聚焦备份恢复安全、Windows Excel/WPS 工作簿所有权和 Excel 大规模导出
性能；不改变 macOS Apple Silicon 主平台定位，Windows 特有行为继续隔离在 platform
层。以下门槛未全部关闭前，只能标记为预览候选，不能标记为正式生产版。

- [x] 备份恢复拒绝路径穿越、绝对/盘符/UNC、非法项目名、重复 ZIP/manifest、
  manifest 内容不一致和 symlink/junction/reparse point；失败不覆盖已有目录，
  只按本次创建身份清理。
- [x] Windows Excel/WPS 记录 Workbook 与 Application 所有权；用户已有文档不关，
  已有实例不 Quit，所有权未知时保守不关闭。
- [x] Excel 导出完成 10k 同环境 before/after 计时与内存记录；50k 基准保留完整
  现场并如实记录旧版与优化版的 `TIMEOUT / INCOMPLETE`。
- [x] Windows CI 加入普通回归测试；中文/空格路径和文件占用测试保留；真实
  Microsoft Excel COM 明确标记 `PENDING / NOT VERIFIED`。
- [ ] 50k/更大规模完整 Excel 导出基准成功完成，并补齐阶段级峰值内存对比。
- [ ] 真实 Microsoft Excel、macOS Excel/WPS 真机专项和并发路径竞态复核。
- [ ] Windows 代码签名、macOS Developer ID 签名与公证，以及授权脱敏黄金案例。
- [ ] 问题台账 UI 录入、云同步/协作等仍按既定边界暂缓。

## v0.1.18 生产门槛（未全部满足）

v0.1.18 以 Windows x64 生产化与真实工程案例验证为主线：真机完成安装/启动/
退出/卸载、OneDrive 与中文/长路径、文件占用与无权限、DPI 缩放矩阵、
Excel/WPS COM 四模式与导出重算回环、数据库破坏性测试（导入/分析/导出中
强杀、库锁定、目录移动、同步冲突、v0.1.16 迁移升级）以及 14 个真实工程
文件（龙泉多期对下结算）的人工真值黄金案例与六类破坏性校核。以下门槛仍未
全部关闭；关闭前只能标记为预览候选，不能标记为正式生产版。

- [x] Windows x64 原生构建（PE 版本资源 + PerMonitorV2 manifest）与真机
  安装/启动/退出/卸载（用户工程数据不受卸载影响）。
- [x] WPS 表格 COM 四模式（强制 Excel ProgID / 强制 Ket / 回退链 / 无 COM
  降级）真机验证；跳转只读打开、只关自启实例、无进程残留、不误杀用户会话。
- [x] 导出→WPS 打开→重算→保存→复开→与程序 Decimal 结果逐格核对（0 漂移）。
- [x] 数据库破坏性测试 7 场景（导入/分析/导出中强杀、库锁定、项目移动、
  OneDrive 同步冲突、v0.1.16 迁移升级）与项目备份/恢复/完整性 API。
- [x] 真实工程黄金案例 14 文件登记（local_private_data），11 文件全流程，
  A 路合计=人工真值（Decimal 精确），累计=Σ各期，跨期匹配与 6 类破坏变体
  全部检出。
- [x] Excel/WPS COM 纪律修复：GetActiveObject 附着优先、pid 快照判定
  started_here、不调用 CoUninitialize（保护 GUI 线程其他 COM 代理）。
- [ ] **Excel 审核底稿导出在大规模行数下超线性（5 万行 >2 小时未完成），
  需优化后重跑 1万/5万/20万完整基准。**
- [ ] Windows 代码签名证书（构建脚本已留 JIADUN_SIGN_PFX 签名钩子）。
- [ ] 真正的 Microsoft Excel 真机（本机 Excel.Application ProgID 已被 WPS
  接管，无法代表真实 Excel 环境）与 macOS Excel/WPS 复验。
- [ ] 台账字段的人工录入 UI、Word 字段级差异雷达导出。
- [ ] 问题台账 UI 录入、云同步/协作等仍按既定边界暂缓。

在上述未完成项关闭前，v0.1.18 只能标记为预览候选，不能标记为正式生产版。

## 开源首发策略

v0.1.0 先发布源码和 Python wheel，验证真实用户的安装、导入、校核和导出反馈；
签名 DMG、自动发布、Windows 安装包和更多格式支持按真实 Issue/下载数据迭代，
不因追求一次性全平台而延误 Mac 主线。

## v0.1.17 生产门槛（未全部满足）

v0.1.17 在 v0.1.16 真实资料实测基线上继续收紧可信度证据（黄金回归登记、匹配质量基准、解析范围核对和取消/异常现场证据），
不改变解析与计算行为，同时保留不可变 Run Contract、逐 Sheet
覆盖证明、四级状态、受控知识资产、项目版本链和历史综合单价提示。以下生产门槛仍未全部
关闭；在关闭前只能标记为预览/Beta 候选，不能标记为正式生产版。

- [x] 清单明细完整数据可分页到达，导出使用完整数据集。
- [x] 双向校核按“校核充分 / 校核有发现 / 校核不充分”分级，并保留 C
  控制、范围完整性和待确认工作表证据。
- [x] 程序 Decimal 值与审核底稿公式值并列输出，主结论不依赖 Office 重算。
- [x] P0/P1 审核界面：清单分页、三档校核、项目总览、问题中心、匹配对照、真实工作表预览、成果卡片。
- [x] 当前 UI、Excel 和 Word 共用项目四级状态快照，旧 Run Contract 结果退出当前结论。
- [x] 字段映射模板、别名知识、规则配置、Finding 生命周期、项目版本和历史单价采用版本化 Evidence。
- [ ] WPS、macOS Excel、Windows Excel 真机打开与重算专项验证。
- [ ] 1万、5万、20万清单行导入/搜索/异常/匹配/校核/导出基准及可取消长任务。
- [ ] Developer ID 签名、公证、发布 CI、版本迁移/备份恢复和异常退出恢复测试。
- [ ] 真实案例回归完成并由独立复核关闭高影响问题。

在上述未完成项关闭前，版本只能标记为预览/Beta 候选，不标记为正式生产版。
