# ADR-005: 原始文件只读，导入即副本
状态: Accepted | 日期: 2026-08-29
## 决策
导入 = 计算文件 SHA256 + 复制到 <project>/originals/<sha256>_<原文件名>（只读权限），
原始路径仅作记录。所有解析只读副本。导出写入 exports/。
## 理由
最高优先级原则 2：任何情况下不得修改/覆盖/删除用户原始文件。
## 强制手段
导入 API 是唯一入口；originals/ 目录权限设为只读；测试断言导入后原文件字节不变。
