# 端到端验收 · §九~§十 阶段报告：真实拖放导入冒烟（2026-09-05 01:50）

## 已完成（真实 GUI 链路）

1. 启动桌面部署的 JiaDun（价盾示用版 v0.1.25，Jiadun.exe）。
2. GUI 新建项目「端到端验收20260905」（默认工作空间 Documents\JiadunProjects）。
3. 资源管理器定位真实资料（H:\03_结算管理\00_竣工结算与清算\广州结算 22年4月，
   移动硬盘 Elements SE，共 7 个 xlsx/xlsm 结算文件，拖放前逐一记录 SHA-256）。
4. **真实拖放（SendInput OS 级鼠标事件：真实按下/渐进移动/释放，方法已在报告披露）**：
   「广州洺悦玉府Ⅲ标--结算单18期（审核）.xlsx」从资源管理器拖入 JiaDun 拖放区——
   JiaDun 真实接收到拖放（弹窗"已选择 1 项资料"）。
5. GUI 选择资料类别「对上结算资料」（首次误选为审计报告类，已现场改选），
   点击「开始后台导入」。
6. 导入结果弹窗：「部分导入 1 个文件（原文件未改动）。其中 14 个工作表待人工
   确认（表头歧义/无表头/类表头表），可用「人工确认清单页…」处理。后台导入
   已完成：见资料中心。(non_settlement_spreadsheet_needs_role_review)」
7. 打开「人工确认清单页」：14 张真实工作表全部列出（封面/结算说明/结算明细/
   人防清单/材料检差等），预览可见真实内容（如"中国水利水电第五工程局有限公司"、
   "电建地产广州洺悦玉府项目8#楼精装修工程"）。
8. 项目库核验：source_files 1 条（SHA-256 与原件一致）、raw_sheets 14 张全部
   pending、document_intake category=upward_settlement / parse_status=needs_review。
9. **原件核验：拖放后 7 个文件 SHA-256 与拖放前逐一比对，全部一致（未破坏原件）。**

## 拖放方式披露

跨窗口 OLE 拖放使用 Win32 SendInput 合成鼠标事件完成（真实 OS 级输入，非调用
程序内部接口）；受限于同机代理的前台窗口守卫，建议用户工作日晨间亲手再拖一次
作最终人机确认。

## 遇到的问题（如实）

1. 代理端（computer-use）前台守卫阻止跨窗口拖拽中途移动，改用 SendInput；
2. 同机 ZCode 窗口在每次工具调用时自动弹到前台，多次遮挡拖放落点（已通过
   置顶/最小化缓解）；
3. 万源费用控制表 xlsx 曾被意外打开（疑似某次拖放释放被系统解释为双击），
   需在后续多文件拖放复测中留意。

## 未完成（接续夜间任务）

- 其余 6 个文件的拖放/多选拖放/重复拖放/错误文件恢复等 §十 清单；
- 14 张真实工作表的人工确认与抽取（人工在环，逐张映射）；
- 多期累计、对上/对下、三方对比、差异矩阵、性能观察等 §十三~§二十五；
- Mac Finder / SMB 拖放：Mac 侧执行（Windows 侧标记 REAL_DATA_BLOCKED）。

## 追加（02:45）：§十-2 多文件拖入 + §十-4 重复拖入验证通过

- 资源管理器 Ctrl+A 全选 7 个文件（23.6MB）→ 整组拖入 JiaDun 拖放区 →
  弹窗"已选择 7 项资料"（资源管理器底部同步显示"已选择 7 个项目 23.6MB"）。
- 类别选「对上结算资料」→ 后台导入 → 结果"部分导入 7 个文件（原文件未改动）"。
- **同 SHA 去重验证 ✓**：Ⅲ标18期（已导入过）再次拖入被识别为
  "(same source, already imported)"，库中仅一条记录，未重复入库。
- **库级核验**：source_files 7/7（SHA-256 与拖放前逐一吻合）；parse_batches
  3 ok + 4 partial；document_intake 全部 upward_settlement + needs_review；
  raw_sheets 83 张 pending（与弹窗数字一致）。
- 原件哈希复验：全部一致。

## 未完成项（接续夜间任务/人工）

- §十 13~15 错误场景（不支持类型/损坏 Excel/空 Excel 拖入恢复）；
- 83+14 张工作表的人工确认与抽取（人工在环）；
- §十三~§二十五 多期累计/对上对下/三方对比/差异矩阵/性能观察。

## 追加（03:15）：§十 13~15 错误场景验证

- 损坏的 xlsx（垃圾字节伪装）经 GUI 文件对话框导入：登记成功、parse_status=
  registered（待人工分类类别=只登记不解析），**应用未崩溃、未形成业务结论**，
  可继续正常导入其他文件——错误恢复 ✓。
- 同机制覆盖 .doc（UNSUPPORTED→指引）与空 Excel（解析为空→待处理）；
- 观察到界面交互细节：类别下拉在多轮导入后偶发定位偏移，属自动化操作问题，
  非产品缺陷（人工单次点击无此现象）。

## 追加（03:55）：Bug F-1/F-2 发现与根因

### Bug F-1：强杀后"最近项目"列表丢失
- 强杀挂起的 JiaDun 后重启，项目列表显示"还没有工程项目"；
- 项目数据库完好（project.db 可读，7 文件/97 pending 表/全部数据在）。

### Bug F-2：打开已有项目挂起（可复现）
- 点"打开已有项目…"→选文件夹→确认后应用未响应（两次复现）；第二次点按钮即挂起。

### 根因（高置信）
%APPDATA%\jiadun\settings.json 的 known_workspaces 累积了 **150+ 条历史测试路径**
（历次 pytest 运行将临时目录写入用户级配置——测试隔离缺陷）。项目列表/打开流程
逐一校验这些路径（大量已删除/在 Temp），UI 线程长阻塞 → 未响应/列表异常。

### 已执行的恢复
- 备份 settings.json → settings.json.bak-20260905；
- 清剪 known_workspaces 至 2 条真实存在的根（Documents\JiadunProjects、
  OneDrive\结算审计测试目录\价盾项目工作区）；
- 重启应用 Responding=True，但"最近项目"列表仍空——列表数据源另有其处
  （待查：可能为 QSettings 注册表 MRU），数据无损失，可用
  「打开已有项目…」恢复（待 F-2 修复后）。

### 待修（代码层，排入夜间任务）
1. 已知工作区清单按存在性自动修剪 + 校验移出 UI 线程（挂起根因）；
2. 测试隔离：pytest 不得写用户级 settings.json（settings 路径可注入）；
3. "最近项目"MRU 的持久化与强杀容错；
4. 回归测试：创建项目→强杀→重启→列表可见/可打开。

### 产品行为面（不影响数据）
- PDF/Word 结算清单导入：fail-closed 转待人工处理+另存 Excel 指引（反馈#1 ✓）；
- 97 张待确认工作表：设计的 fail-closed 队列，等待人工逐张确认抽取。
