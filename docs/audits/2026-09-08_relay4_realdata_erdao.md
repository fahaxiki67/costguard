# 接力四轮：真实使用场景测试与最小修复（二道沟语料）

- 日期：2026-09-07 深夜 → 2026-09-08 凌晨（收口记录）
- 范围：隔离接力副本 `projects/costguard-main`；main `ae31192` 起点
- 敏感性：本报告不含真实工程金额、路径与资料内容；完整对照数据仅在
  `local_private_data/relay_20260907/ROUND_REPORT_20260908.md`（不入 Git）

## 一、基线记录

- 起点 main @ `ae31192`；既有未提交 diff 仅 `tests/unit/test_ui_workbench.py`
  （上轮 TDD 红测试，快照 SHA-256 `475023f8…`，全程未覆盖未回滚）。
- 全量 pytest：984 通过 / 2 失败 / 3 跳过。失败 = 继承红测试 +
  `test_reporting_summary` 一项偶发失败（单跑必过，见 §五）。

## 二、代码修复（3 个提交）

### 251ce89 feat(ui): 全工作簿 Sheet 清单对话框 + 期次操作两行布局

- 根因 1：v52 数据层 `sheet_inventory.py` 从未接线 UI——继承测试期望的
  「全工作簿 Sheet 清单…」按钮不存在（用户反馈#2：几十个 Sheet 定位困难）。
- 根因 2：期次页 10 个操作按钮单行 QHBoxLayout，窄窗口压缩/裁切。
- 最小修复：新对话框（只读全部 Sheet + 过滤 + 清单类型人工标注，理由必填
  写 Evidence，建议仅为候选）；按钮拆两行；新增 `test_ui_sheet_inventory.py`。

### fec3a0b feat(acceptance+ui): 执行器合同 PDF 走 GUI 同路径 OCR + 状态原因提示

- 覆盖缺口（真实使用 bug 类）：验收执行器对合同 PDF 只做轻量文本解析，
  未走 GUI 实际使用的 `import_contract`+OCR 路径——同一文件 GUI 能 OCR、
  验收永远 pending_ocr，"开发验收 ≠ 真实使用"。
- 最小修复：pdf 分支改走 `import_contract`+`get_default_ocr_provider()`；
  `PdfExtractionPending` 时记录页级状态计数与 provider 元数据（部分完成
  不得写成整档成功）；新增文本 PDF 构造器锁定接线。
- Sheet 清单对话框状态列 tooltip 携带 `sheet_status_reason`：真实数据实测发现
  "确认抽取后仍有结构性缺口"的 Sheet 在清单里只显示"待确认"，用户无法
  看到原因（原因本已落库）。

## 三、真实资料验收（语料 T-ERG-01..15，双端哈希全程一致）

协议：`docs/REAL_DATA_ACCEPTANCE.md`；执行器 `scripts/real_acceptance_run.py`。
四轮 run（run_20260907_235217 → run_20260908_001652），历史轮全部保留。

- 15/15 导入成功；每轮副本 SHA-256 处理前后一致（无一副本被修改）。
- 首轮全部 honest 分级：0"技术完成"，无一处把 PENDING 写成 PASS。
- 经有人工依据的 sheet 决策（列映射/范围/理由落 Evidence），一个真实结算书
  （T-ERG-08，F.1 清单页 188 行）走通全链路：
  - Decimal 复算 148 组；A 路径（清洗明细）= B 路径（原始网格重算）=
    C 路径（原表合计控制值），三路差异 0.00；
  - 区块标签行 15、分部小计 16 正确排除；46 行缺失字段显式标记，未补零；
  - 异常规则 47 条（缺关键字段/同名异码/同码异名/文本数字/数据区合并等）；
  - 匹配 134 项全部带置信档；证据 224/224 可溯源；
  - 导出 xlsx+docx 重读核验通过（19 表结构、逐项值、摘要合计值）；
  - 项目级结论诚实保持"不可形成项目结论"（尚有页未闭合）；
    WPS/Excel 三环境人工复核仍为 PENDING。
- 重放稳定性：同决策第三轮重放，结果字节级一致。

## 四、与两份参考文件对照（口径级，细节金额见私有报告）

- 结算书汇总页合计 ↔ 大表"审减汇总"市政景观审定值：**精确一致**。
- 抽取行 vs 审核报告审减明细表行级抽查 4/4 精确一致（审定量口径）。
- 真零/缺失区分实证：某核减为 0 的项目在原表为字面 0 量 + 空价额，
  系统抽取为真零数量 + NULL 价额，未补零。
- 清单层合计与汇总层合计的差额（措施/规费/税等）在两份参考文件中无
  组成明细 → 登记"待补资料/不可比"，不调平。
- 对上审定、对下开累、办理进度三个口径并存，系统不做自动调和（正确行为）。
- 台账类文件（多期次×分组）确认不适用"单期次清单"模型，保持角色确认门控
  ——这是能力边界，不是缺陷；强推会违背"无法比较不得强行比较"。

### 收口复核：候选控制值与人工桥接分开

- 后续私有验收语料扩展为 23 个文件（原 15 个二道沟语料，加上 XHG/SJ 次级样本）；
  完整文件清单、哈希和运行结果只保留在 `local_private_data/`，不入 Git。
- 复核确认：结算清单层的 C 值只能标为**候选/当前文件控制值**，必须同时记录实际来源；
  它不能直接替代汇总层（如 E.4）控制值，也不能据此形成正式结算结论。
- 验收报告已改为显示 `C/候选控制`、`C来源`，并单列“人工控制桥接与差异”；桥接、组成/重复排除、
  业务范围等未闭合事项保持开放，不调平、不覆盖原始值。
- 人工决定完成后，报告改为从当前 `raw_sheets`/`line_items` 刷新工作表状态和实际行数：
  非业务汇总页显示为“非业务表”，结算清单显示为“已确认抽取，待结构/范围复核”，不再使用人工确认前的 0 行快照。
- 总体表将 `A/B独立复算` 改为 `A/B结果一致性`，并明确“共享抽取器，独立性未证明”；数值一致不再被表头暗示为独立复核通过。
- 独立复核结论为：技术演示链路可复现，但正式参考口径仍为条件性、不通过；需完成汇总层桥接、
  范围/角色语义复核及 WPS/Excel 实机复核后，才能重新评估正式用途。

### 跨项目清单页探针补充（独立人工探针）

- 23 个文件混合语料之外，新增两页只读人工确认探针：西航港“计量计价清单明细”和双江
  “1#变更（土建工程）”。两页均在临时隔离项目中各独立重放两次，候选行数、状态、范围和结构风险一致，
  原始副本哈希未变化。
- 西航港页修复后抽取 105 行候选，其中 102 行工程量明细、1 行税金/费用、2 行汇总；真实表中的“合计（含税/不含税）”位于首列，
  已补充税口径后缀识别并保留为汇总行，避免进入明细累计。筛选范围、隐藏列、明细区合并单元格及外部公式缓存
  未验证问题均被保留，因此仍为 `partial/pending`。
- 双江变更页抽取 139 行候选明细；明细区合并单元格和除零公式缓存已保留，筛选/隐藏状态影响尚未证明，
  因此仍为 `partial/pending`。
- 该探针只证明“人工映射后可重复进入候选抽取并正确保留门控”，不证明跨期、变更归属、汇总勾稽或正式金额。
  它不是最新完整验收自动完成的结算链路；完整运行对这两页仍保持 `parse=false`/`needs_manual_review`。
- 该修复按真实形态先写红测试再改最小共享识别规则；修复前红测试失败，修复后同一测试及相关抽取/管线测试通过。

## 五、OCR 真实质量第一轮实测（RapidOCR 本地引擎）

- 3 份真实扫描合同 PDF：23 页 / 118 页 / 2 页；OCR 成功 18/104/1 页；
  低置信 needs_review 5/14/1 页；0 页 ocr_failed。
- 有 needs_review 页的文档整档 fail-closed，不产出合同事实候选；
  后续走逐页人工复核（page_review 既有通道）。
- 局限：needs_review 判定阈值与人工复核工单化未测；Paddle 增强 provider
  未测（需本地模型清单）。

## 六、未闭合事项

1. **flaky**：`test_report_model_has_one_shared_summary_and_no_approval_upgrade`
   全量轮次偶发失败（6 轮中 1 次），单跑 20 个 PYTHONHASHSEED 均过；
   未捕获失败堆栈，不定根因、不改代码——继续带全量日志观察。
2. WPS/Excel 三环境实机复核（导出重算、超链接、打印）——需人工，PENDING。
3. 台账/汇总类文件的结构化建模（如需）属新需求，待产品决策。
4. 其余 12 个语料文件保持诚实门控，待逐个人工决策后扩链路。

## 七、验证与回滚

- 全量 pytest 见冻结记录；ruff check 通过（未整文件 format churn，
  与既有文件风格一致的最小 diff）。
- 收口改动的针对性测试覆盖验收报告的候选控制/桥接展示，以及 Sheet 清单的
  “已确认抽取，待结构/范围复核”状态、人工决定后的数据库快照刷新，以及共享抽取器的 A/B 语义；
  修复后最新私有运行（`run_20260908_022047_096540`）仍标记 `pending_wps_with_findings`，
  23/23 文件导入成功且处理前后副本哈希一致；T-ERG-08 保持 148 组复算、47 条异常、226/226 证据可溯源。
- 回滚：revert `251ce89`、`fec3a0b` 即可；无 schema/migration 变更，
  无数据口径变化（direction 标注错误在验收数据层修正并留痕，未入代码）。
